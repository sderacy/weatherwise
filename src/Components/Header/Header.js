import SearchBar from "../SearchBar/SearchBar";
import Title from "../Title/Title";

const Header = () => {
  return (
    <header>
      <Title />
      <SearchBar />
    </header>
  );
}

export default Header;
